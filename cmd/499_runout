##################################################################################
# Run command outside bin directory
#
# ** Description **
#   Shortcut:
#&    ${DESPATCH_SCRIPT} -
#
#   Parameters
#     [1]: Script name, default sh(.sh)
##################################################################################
if [ ! -f cli.sh ]; then
  cmn_exitAbnormal "There is no script cli.sh"
else
  . cli.sh
fi
